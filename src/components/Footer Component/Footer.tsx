import React from "react";
import styles from "./Footer.module.css"

const FooterComponent: React.FC = () => {
    return <div className={styles.footer + " FooterComponent"}>Footer</div>
}

export default FooterComponent

